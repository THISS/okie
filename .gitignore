node_modules/
.pnpm-store/
dist/
target/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.playwright-cli/

# Generated wasm-pack output. Rebuild it from crates/atlas-wasm.
crates/atlas-wasm/pkg/

# Large deterministic benchmark scenes are generated on demand.
fixtures/renderer/stress-*.json

# Deterministic scan output (regenerate with `okie-scan`).
fixtures/scan/

# Local env files and credential artifacts — never commit real keys.
.env
.env.*
okie.local.json
*.pem
*.key
id_rsa
id_ed25519
.npmrc


# Relocatable static viewer build.
apps/web/dist-portable/

# Local review backups; never ship archived working copies.
.okie-review/
