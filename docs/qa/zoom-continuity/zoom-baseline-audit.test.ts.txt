import {readFileSync,writeFileSync} from 'node:fs';import {it} from 'vitest';
import {buildC4ProjectionBundle} from '@okie/architecture';
import {compileScanFixture,guardScanCompile} from './src/renderer/scanFixture';
it('locates focus dependent canonical root offset',()=>{
const snapshot=JSON.parse(readFileSync('/tmp/zoom-coverage-snapshot.json','utf8')),view=JSON.parse(readFileSync('/tmp/zoom-coverage-view.json','utf8')),story=JSON.parse(readFileSync('../../fixtures/scan/thiss__okie/story.json','utf8'));const fixture=compileScanFixture({snapshot,view,story},{targetAspect:1.6});
const rows=['container:apps-web','component:apps-web-src-hosted-atlas-ts','component:apps-web-src-renderer-fixture-bundle-ts','component:apps-web-src-inspector-scan-one-pager-ts'].map(focusEntityId=>{
const guard=guardScanCompile(snapshot,focusEntityId,view.rootEntityId);const bundle=buildC4ProjectionBundle(snapshot,{rootEntityId:view.rootEntityId,focusEntityId,familyId:'audit',...guard.options,targetAspect:1.6});const final=fixture.createScene(focusEntityId);const ids=['system:okie','container:apps-web','component:apps-web-src-hosted-atlas-ts'];const bounds=ids.map(id=>{const vid=bundle.index.visualNodeIdsByEntityId[id]?.[0];return{id,before:Object.fromEntries(['context','container','component','code'].map(band=>{const p=bundle.projectionById[bundle.family.projectionIds[band]];return [band,vid?bundle.bandLayoutById[p.layoutId].nodes[vid]??null:null]})),after:final.projection?.boundsByEntityIdAndDetail[id]};});return{focusEntityId,options:guard.options,bounds,contextNodes:bundle.projectionById[bundle.family.projectionIds.context].visualNodeIds.map(id=>({id,kind:bundle.visualNodeById[id].kind,bounds:bundle.bandLayoutById[bundle.projectionById[bundle.family.projectionIds.context].layoutId].nodes[id]}))};});writeFileSync('../../docs/qa/zoom-continuity/root-baseline-audit.json',JSON.stringify(rows,null,2)+'\n');console.log(JSON.stringify(rows));
},60000);
